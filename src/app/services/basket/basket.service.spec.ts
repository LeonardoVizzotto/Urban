import { BasketService } from './basket.service';

describe('BasketService', () => {
  let service = new BasketService();

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
